<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Example.Forms.MainPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:Lottie.Forms;assembly=Lottie.Forms"
             xmlns:local="clr-namespace:Example.Forms">
    <ContentPage.BindingContext>
        <local:MainPageViewModel />
    </ContentPage.BindingContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackLayout Grid.Row="0"
                     Margin="5,0"
                     HorizontalOptions="FillAndExpand"
                     Orientation="Horizontal">

            <Button Command="{Binding StartPlayingCommand}"
                    HorizontalOptions="FillAndExpand"
                    Text="Play" />

            <Button x:Name="playSegmentsButton"
                    HorizontalOptions="FillAndExpand"
                    Text="Segment" />

            <Button x:Name="playFramesButton"
                    HorizontalOptions="FillAndExpand"
                    Text="Frames" />

            <Button Command="{Binding StopPlayingCommand}"
                    HorizontalOptions="FillAndExpand"
                    Text="Pause" />
        </StackLayout>

        <forms:AnimationView x:Name="animationView"
                             Grid.Row="1"
                             Animation="LottieLogo1.json"
                             HorizontalOptions="FillAndExpand"
                             IsPlaying="True"
                             Loop="True"
                             VerticalOptions="FillAndExpand" />

        <Slider Grid.Row="2"
                Margin="5,0"
                ValueChanged="Slider_OnValueChanged" />
    </Grid>
</ContentPage>